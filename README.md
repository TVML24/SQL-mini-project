# SQL-mini-project
A miniproject designed to allow practice of expressJS and SQL


SELECT * FROM (table you want to join)
JOIN book_prices(table we want to join it to)
WHERE favorite_books.book_price = book_price.id
(where = tells SQL which part of the table to make the join at)
()